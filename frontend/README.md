# PathMapper Frontend

This is the React + Vite frontend for PathMapper.

## Install dependencies

```bash
npm install
```

## Run locally

```bash
npm run dev -- --host
```

The app will be available at the URL shown in the terminal.
